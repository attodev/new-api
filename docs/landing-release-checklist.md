# 랜딩페이지 최종 릴리즈 체크리스트

> 목적: 랜딩페이지의 릴리즈 전 필수 수정과 후속 개선 작업을 한곳에서 관리한다.
>
> 사용법: 구현만 끝났을 때가 아니라 아래 완료 조건과 회귀 검증까지 통과한 뒤 `[x]`로 변경한다. 관련 커밋 또는 PR이 있으면 각 항목 아래 기록한다.

## 진행 현황

- [ ] 1단계 — 릴리즈 필수 작업 완료
- [ ] 2단계 — 적용하면 확실히 좋은 개선 완료
- [ ] 3단계 — 장기 유지보수 개선 완료
- [ ] 최종 운영 검증 완료

예상 공수는 1명이 구현, 코드 리뷰 대응, 한국어/영문 및 데스크톱/모바일 회귀 테스트까지 수행하는 기준이다.

## 1단계: 최종 릴리즈 전에 꼭 해야 하는 작업

예상 합계: **5~7영업일**

### P0. HTML과 정적 자산 캐시 정책 분리 — 0.5~1일

- [x] `/`, `/en`, `/guide`, `/en/guide` 등 모든 HTML 응답에 재검증 가능한 캐시 정책을 적용한다.
- [x] 고정 파일명의 이미지, 영상, CSS, JS에 콘텐츠 해시 기반 ETag 재검증 전략을 적용한다.
- [x] 콘텐츠 해시가 파일명에 포함된 빌드 자산에만 장기 캐시를 허용한다.
- [ ] 배포 직후 한국어와 영문 페이지가 모두 최신 콘텐츠를 표시하는지 확인한다.
- [ ] 동일 파일명의 포스터·영상 교체가 이전 캐시로 남지 않는지 확인한다.

완료 조건:

- HTML은 배포 후 즉시 최신 버전을 재검증한다.
- 버전이 변경된 정적 자산은 이전 캐시와 충돌하지 않는다.
- 한국어와 영문 HTML의 캐시 정책이 일관적이다.

관련 기록:

- 상태: 구현 및 로컬 검증 완료, 운영 배포 검증 대기
- 테스트: `go test ./middleware ./router`
- 로컬 응답: 고정 이름 CSS·poster `no-cache, must-revalidate` 및 ETag, 재검증 요청 `304`
- 로컬 응답: 해시된 SPA 자산 `public, max-age=31536000, immutable`
- 안전 보완: `/static/` 아래에서 파일명에 콘텐츠 해시가 있는 빌드 자산만 immutable 허용
- 커밋/PR: 이 체크리스트 갱신을 포함한 캐시 정책 커밋

### P0. 정적·영상 요청을 글로벌 rate-limit에서 제외 — 0.5~1일

- [x] 이미지, 영상, CSS, JS, 폰트 요청을 글로벌 웹 요청 제한에서 제외한다.
- [x] HTML 및 동적 엔드포인트에는 적절한 제한을 유지한다.
- [x] 영상 Range 요청이 일반 요청 제한을 불필요하게 소모하지 않도록 한다.
- [x] 강력 새로고침과 언어 전환을 반복해도 정적 파일에서 `429`가 발생하지 않는지 확인한다.
- [x] API 요청 제한이 변경 전과 동일하게 동작하는지 확인한다.

완료 조건:

- 한국어/영문 전환과 새로고침을 각각 10회 이상 반복해도 화면 자산이 정상 로드된다.
- 동적 요청에 필요한 보호는 유지된다.

관련 기록:

- 상태: 구현 및 로컬 회귀 검증 완료, 운영 배포 검증 대기
- 정책: `GET`/`HEAD` 정적 확장자만 제외하며 HTML, 확장자 없는 동적 경로, JSON 경로, 쓰기 요청은 제한 유지
- 테스트: `go test ./middleware ./router`
- 로컬 반복 검증: CSS·이미지 각 10회 `200`, 영상 Range 10회 `206`, 제한값 2에서 HTML `200`, `200`, `429`
- 한·영 회귀 검증: `/`, `/en`, `/guide`, `/en/guide`와 핵심 정적·Range 요청을 10회 반복해 `429` 없음
- 커밋/PR: 이 체크리스트 갱신을 포함한 정적 자산 rate-limit 분리 커밋

### P0. 가격 모델명 DOM 주입 방지 — 0.25일

- [ ] 가격 계산기에서 API 모델명을 `innerHTML`로 삽입하지 않는다.
- [ ] 문의 팝업에서도 동일하게 안전한 DOM API 또는 `textContent`를 사용한다.
- [ ] HTML 특수문자가 포함된 모델명이 텍스트로만 표시되는지 테스트한다.

완료 조건:

- 모델명 데이터가 마크업이나 스크립트로 해석되지 않는다.

관련 기록: `커밋/PR: `

### P1. 포스터 최적화 및 LCP 개선 — 1~2일

기준값: 운영 모바일 4G 유사 조건에서 LCP 약 **13.3초**, 초기 동일 출처 전송량 약 **2.4MB**.

- [ ] 최초로 표시할 포스터 한 장만 높은 우선순위로 로드한다.
- [ ] 대체 포스터는 idle 시점, 사용자 전환 의도 또는 전환 직전에 로드한다.
- [ ] 포스터를 WebP/AVIF 등 효율적인 형식으로 제공한다.
- [ ] 모바일 화면에 맞는 해상도와 `srcset`/`sizes` 적용 여부를 검토한다.
- [ ] 첫 화면에서 불필요한 영상 metadata 요청을 줄인다.
- [ ] 기존의 원자적 poster 전환과 실패 fallback 동작을 유지한다.
- [ ] 느린 네트워크에서 영상 메뉴를 반복 전환해도 깨진 이미지나 alt 문구가 나타나지 않는지 확인한다.
- [ ] 운영 유사 조건에서 LCP와 초기 전송량을 다시 측정한다.

완료 조건:

- 목표 LCP는 모바일 4G 유사 조건에서 **4초 이하**로 한다.
- 최소 완료 기준은 기존 13.3초 대비 원인이 설명 가능한 명확한 개선과 poster 전환 오류 0건이다.

관련 기록: `커밋/PR: `

### P1. 문의·법적 문서 모달 접근성 — 0.75~1일

- [ ] 모달에 `role="dialog"`, `aria-modal`, 제목 연결을 적용한다.
- [ ] 모달을 열면 제목 또는 첫 입력 요소로 포커스를 이동한다.
- [ ] 모달 내부에 포커스를 가둔다.
- [ ] Escape 키로 모달을 닫을 수 있게 한다.
- [ ] 모달을 닫으면 실행 버튼으로 포커스를 복귀한다.
- [ ] 열린 동안 배경 콘텐츠를 `inert` 또는 동등한 방법으로 접근 불가 처리한다.
- [ ] 닫기 버튼에 명확한 접근성 이름을 제공한다.
- [ ] 한국어/영문 문의 모달과 개인정보·약관 팝업을 모두 검증한다.

완료 조건:

- 마우스 없이 Tab, Shift+Tab, Enter, Escape만으로 모달을 완전히 사용할 수 있다.

관련 기록: `커밋/PR: `

### P1. 영상·입력·언어 전환 접근성 — 0.75~1일

- [ ] 영상 재생·일시정지 컨트롤을 키보드 조작 가능한 버튼으로 구현한다.
- [ ] 영상 컨트롤에 현재 동작을 설명하는 `aria-label`을 제공한다.
- [ ] 계산기와 문의 폼의 모든 label을 입력 요소와 연결한다.
- [ ] 언어 전환 버튼에 명확한 접근성 이름을 제공한다.
- [ ] Tab 순서가 화면의 시각적 흐름과 일치하는지 확인한다.

완료 조건:

- 접근성 트리에서 모든 입력과 핵심 버튼에 이름이 존재한다.
- Enter/Space로 영상과 언어 전환을 사용할 수 있다.

관련 기록: `커밋/PR: `

### P1. Sitemap 및 명백한 콘텐츠 오류 수정 — 0.25~0.5일

- [ ] Sitemap의 영문 URL을 canonical `/en`으로 변경한다.
- [ ] `/guide`, `/en/guide`를 Sitemap에 추가한다.
- [ ] 개인정보처리방침과 이용약관 URL을 실제 `/legal/...` 경로와 일치시킨다.
- [ ] Sitemap의 모든 URL이 정상 응답하고 canonical과 일치하는지 확인한다.
- [ ] 한국어 `총 달당량` 문구를 의도에 맞는 용어로 교체한다.
- [ ] `Total Budget`이 실제 지표 의미와 일치하는지 확인하고 한국어와 함께 정리한다.

완료 조건:

- Sitemap에 오래된 URL이나 SPA fallback URL이 없다.
- 한국어와 영문의 지표 의미가 동일하다.

관련 기록: `커밋/PR: `

### 1단계 통합 검증 — 1일

- [ ] 한국어 데스크톱 전체 화면을 점검한다.
- [ ] 영문 데스크톱 전체 화면을 점검한다.
- [ ] 한국어 모바일 전체 화면을 점검한다.
- [ ] 영문 모바일 전체 화면을 점검한다.
- [ ] 메뉴, 언어 전환, 영상 전환, 계산기, 모달, footer 링크를 점검한다.
- [ ] 브라우저 콘솔 오류와 경고가 없는지 확인한다.
- [ ] 네트워크 실패, `404`, `429`, 잘못된 캐시 응답이 없는지 확인한다.
- [ ] 키보드만으로 주요 사용자 흐름을 수행한다.
- [ ] 모바일 성능을 다시 측정하고 기준값과 비교한다.

관련 기록: `검증 환경/결과: `

## 2단계: 꼭까지는 아니지만 적용하면 확실히 좋은 작업

예상 합계: **3~5영업일**

### 접근성과 시각 품질

- [ ] 낮은 명도 대비의 보조 텍스트 색상을 조정한다. — 0.5~1일
- [ ] 지나치게 작은 보조 텍스트의 크기와 행간을 조정한다.
- [ ] CTA 그라디언트 위 텍스트 대비를 확보한다.
- [ ] 링크와 버튼에 일관된 `:focus-visible` 스타일을 적용한다. — 0.25~0.5일
- [ ] `prefers-reduced-motion` 환경에서 불필요한 모션을 제거한다. — 0.5일
- [ ] JS 또는 IntersectionObserver 실패 시에도 콘텐츠가 보이도록 한다.
- [ ] `#pricing` 등 해시 직접 진입 시 콘텐츠가 처음부터 보이는지 확인한다. — 0.25일

관련 기록: `커밋/PR: `

### 모바일 UX

- [ ] 모바일 히어로 높이와 상하 여백을 줄인다. — 0.75~1.5일
- [ ] 모바일에서 CTA와 영상 사이의 거리를 줄인다.
- [ ] 통계 3개가 어색한 `2+1` 배치로 남지 않도록 개선한다.
- [ ] 챗봇 버튼이 영상, CTA, 중요 콘텐츠를 가리지 않도록 한다. — 0.25일
- [ ] 모바일 메뉴를 Escape 키로 닫을 수 있게 한다. — 0.25~0.5일
- [ ] 모바일 메뉴의 포커스 이동과 닫힌 후 복귀를 정리한다.

관련 기록: `커밋/PR: `

### 영상 재생 UX

- [x] 서비스 소개 영상은 데스크톱 클릭과 모바일 탭 한 번으로 재생·일시정지를 즉시 전환한다.
- [x] 서비스 소개 영상은 재생 중 중앙 오버레이를 숨기고 포인터로 클릭 가능 여부만 안내한다.
- [x] 서비스 소개 영상의 재생·일시정지 영역을 시맨틱 버튼과 한국어·영문 접근성 이름으로 제공한다.
- [x] CTA, 다시 보기, 영상 버전 전환 동작과 재생 토글이 충돌하지 않도록 레이어를 분리한다.
- [x] 사용 가이드 영상은 네이티브 탐색·음량·전체화면 컨트롤을 유지한다.
- [x] 사용 가이드의 큰 재생 버튼을 시맨틱 버튼으로 변경하고 시작·이어보기 상태를 동기화한다.
- [x] 매뉴얼/CC Switch 탭 전환 시 기존 영상 정지·초기화 정책을 유지한다.
- [x] 브라우저 탭이 백그라운드로 이동하면 서비스 소개와 가이드 영상을 일시정지하고 자동 재개하지 않는다.
- [x] 이전 7일 캐시를 보유한 방문자도 새 컨트롤을 받도록 변경된 CSS·JS URL에 릴리즈 버전 키를 적용한다.

관련 기록:

- 상태: 구현 및 로컬 브라우저 검증 완료
- 검증: 한국어/영문 × 데스크톱/모바일, 시맨틱 버튼, 네이티브 컨트롤, 중복 ID, 가로 overflow
- 커밋/PR: 이 체크리스트 갱신을 포함한 영상 재생 UX 커밋

### 계산기·외부 스크립트·콘텐츠

- [ ] 가격 계산기에 로딩 상태를 제공한다. — 0.5~0.75일
- [ ] 가격 모델이 없을 때 빈 데이터 안내를 제공한다.
- [ ] 가격 API 실패 시 오류 안내와 재시도 방법을 제공한다.
- [ ] 가격 API를 섹션 진입 또는 사용자 의도 시점에 지연 로드한다. — 0.25~0.5일
- [ ] 한국어 CTA의 `체험해보기`와 `체험 신청하기` 사용 기준을 통일한다. — 0.25일
- [ ] 영문 footer에 필요한 회사·법적 정보 범위를 확인하고 한·영 구조를 정리한다. — 0.25~0.5일
- [ ] 챗봇 스크립트를 비동기로 로드하고 실패 영향을 격리한다. — 0.25~0.5일
- [ ] OG 이미지를 압축하고 이미지 크기·대체 텍스트 메타를 보강한다. — 0.25일

관련 기록: `커밋/PR: `

## 3단계: 천천히 진행해도 괜찮은 작업

예상 합계: **5~9영업일**

- [ ] 한국어와 영문 랜딩 템플릿의 공통 마크업을 partial/component로 분리한다. — 2~4일
- [ ] 언어별 콘텐츠만 별도 데이터로 관리한다.
- [ ] inline style과 inline `onclick`을 CSS·JS로 이동한다. — 1~2일
- [ ] 랜딩 계산기와 문의 팝업의 가격 계산 로직을 공통화한다. — 0.5~1일
- [ ] 하드코딩된 가격 배율을 공통 설정 또는 서버 응답으로 관리한다.
- [ ] 링크, Sitemap, 접근성 검사를 자동화한다. — 1~2일
- [ ] Lighthouse 성능 예산을 CI에 추가한다. — 1일
- [ ] 포스터 이미지 생성·압축·버전 부여 과정을 자동화한다. — 1일

관련 기록: `커밋/PR: `

## 최종 운영 검증

- [ ] 테스트 환경에서 캐시와 rate-limit 설정을 검증한다.
- [ ] 운영 배포 직후 한국어와 영문 페이지가 최신 버전인지 확인한다.
- [ ] 운영에서 데스크톱·모바일 핵심 흐름을 smoke test 한다.
- [ ] 포스터 전환 중 이전 이미지 잔류, 깨진 이미지, alt 문구가 나타나지 않는지 확인한다.
- [ ] 실제 응답 헤더에서 HTML과 정적 자산의 캐시 정책을 확인한다.
- [ ] 정적·영상 요청에 `429`가 발생하지 않는지 확인한다.
- [ ] 콘솔 오류와 실패한 네트워크 요청이 없는지 확인한다.
- [ ] LCP, CLS, 초기 전송량을 기록한다.
- [ ] Sitemap과 canonical URL을 다시 확인한다.
- [ ] 최종 릴리즈 승인자를 기록한다.

최종 결과:

- 검증 일시:
- 검증 환경:
- 검증 담당자:
- 릴리즈 커밋:
- LCP:
- CLS:
- 초기 전송량:
- 남은 예외 사항:

## 작업 이력

| 날짜 | 단계 | 작업 | 커밋/PR | 결과 및 비고 |
|---|---|---|---|---|
|  |  |  |  |  |
